# Rove SDK

Shared types, traits, and error definitions used by the engine and plugins. Provides the common interface contracts that all Rove components depend on.

- **Key exports:** `EngineError`, `RoveErrorExt`, `ToolInput`, `ToolOutput`, `ToolDefinition`
