# Rove Scripts

Build and release automation for the Rove project.

- **`build-all.sh`** -- Builds the engine binary and all WASM plugins
- **`release.sh`** -- Full release pipeline: test, clippy, fmt, build, and manifest generation
